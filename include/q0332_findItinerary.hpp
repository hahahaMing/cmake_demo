//
// Created by ming on 2020/8/27.
//

#ifndef CMAKETEST_Q0332_FINDITINERARY_HPP
#define CMAKETEST_Q0332_FINDITINERARY_HPP

#endif //CMAKETEST_Q0332_FINDITINERARY_HPP
#include"tools.hpp"
class Solution {
public:
    std::vector<std::string> findItinerary(std::vector<std::vector<std::string>>& tickets) {
        /*
         * 存一个map，直接按自然排序排好
         * 同时存一个map，表示是否用过，拷贝一份初始化的
         * 从map里取一个，然后递归。。。直到能返回
         */

    }
    void test(){
        std::cout<<"test start"<<std::endl;

    }
};